#pragma once

// This will load the definition for common Particle variable types
#include "Particle.h"

class Coffeemessages {
public:
  // Constructor
  Coffeemessages();

  void proclaim(String message);

private:
  void secret();
};
